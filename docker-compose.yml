services:        # Servicios del proyecto
  web:           # Nombre del contenedor
    build: .     # Construye usando el Dockerfile actual
    ports:
      - "8001:8000"  # PC:8001 → Contenedor:8000
    volumes:
      - .:/app   # Sincroniza tu carpeta con /app dentro del contenedor